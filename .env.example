# Obtain official Telegram API credentials for the analyst's own account.
# Set these locally; never commit values or a Telegram session file.
SYOSINT_TELEGRAM_API_ID=
SYOSINT_TELEGRAM_API_HASH=
# Optional override for the local private-data directory.
SYOSINT_PRIVATE_DIR=
